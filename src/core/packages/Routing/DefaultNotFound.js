import React from 'react'

export default (props) => (
  <div>
    <h1>Not found</h1>
    <p>This page was not found.</p>
    <p>You can override this page using <code>routeNotFoundPage</code> hook.</p>
  </div>
)
